# Data-Cleaning-Project
The dataset was cleaned by handling missing values, removing duplicates, standardizing text formats, correcting date formats, renaming columns for consistency, and ensuring all data types were accurate.
